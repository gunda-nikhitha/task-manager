# TaskFlow — Task Manager App

A clean, minimal task manager built with vanilla HTML, CSS, and JavaScript. No frameworks, no dependencies — just pure front-end skills.

## Live Demo
> Add your GitHub Pages link here after deploying (step 5 below)

---

## Features

- Add tasks with a title, due date, and priority level (low / medium / high)
- Mark tasks as complete with a single click
- Due date badges — shows **Overdue**, **Today**, **Soon**, or the date
- Filter tasks by: All · Active · Completed · Overdue
- Clear all completed tasks at once
- Stats bar showing total, done, and overdue counts
- **Data persists in localStorage** — your tasks survive page refresh
- Fully responsive — works on mobile and desktop

---

## Tech Stack

| Layer    | Technology              |
|----------|-------------------------|
| Markup   | HTML5                   |
| Styling  | CSS3 (custom properties, grid, flexbox) |
| Logic    | Vanilla JavaScript (ES6+) |
| Storage  | localStorage API        |
| Fonts    | Google Fonts (Inter + Space Grotesk) |

---

## Project Structure

```
task-manager/
├── index.html   ← App structure and layout
├── style.css    ← All styles, variables, responsive design
├── app.js       ← Task logic, localStorage, filtering
└── README.md    ← This file
```

---

## What I Learned

- DOM manipulation without any framework
- Managing state with localStorage (read, write, parse JSON)
- Dynamic rendering and filtering from a single data array
- CSS custom properties for consistent theming
- Event delegation for efficient list interactions

---

## Run Locally

No install needed. Just clone and open:

```bash
git clone https://github.com/gunda-nikhitha/task-manager.git
cd task-manager
# open index.html in your browser
```

Or simply double-click `index.html` — it runs straight in the browser.

---

## Author

**Gunda Nikhitha** — [GitHub](https://github.com/gunda-nikhitha) · [LinkedIn](#)
